{% extends '::frontenda.html.twig' %}

{% block title %}Administracion Servicios Alimentacion{% endblock %}



{% block article %}


<div class="container sha well ">


     <ol class="breadcrumb text-left alert alert-success">
        <li><a href="{{ path('portadaa') }}">Home</a></li>
        <li class="active ">Administracion Servicios Alimentacion </li>
      </ol>


    {% for mensaje in app.session.flashbag.get('info') %}
        <p class="bg-warning">{{ mensaje }}</p>
    {% endfor %}

        
            <div class="col-xs-12 bg-primary" >
            <h2>Administracion Servicios Alimentacion:</h2> 
            </div>
            <br>
            <br>
            <br>
            <br>
                                          
              {% if serviciosalimentacion %}
              <div class="row">                

                  <a href="{{ path('alimentacion_new') }}" class="btn btn-lg btn-success"> Crear Nuevo</a>
                  <br>
                  <br>
                  <br>

                    
                    <table class="table table-striped">

                      <thead>
                        <tr class="info">
                          <th>Nombre</th>
                          <th>Precio Servicio</th>
                          <th>Detalles</th>
                          <th>Acciones</th>
                        </tr>
                      </thead>

                      <tbody>
                        {% for serviciosalimentacion in serviciosalimentacion %}
                        <tr>
                          <td>{{ serviciosalimentacion.nombre }}</td>
                          <td>${{ serviciosalimentacion.precio|number_format( 0,'', '.') }}</td>
                          <td>{{ serviciosalimentacion.detalles[0:100] }}...</td>
                          
                          <td>
                            <a href="{{ path('alimentacion_show', { 'id': serviciosalimentacion.idalimentacion }) }}" class="btn btn-sm btn-success"> Ver </a>
                           
                           <a href="{{ path('alimentacion_edit', { 'id': serviciosalimentacion.idalimentacion }) }}" class="btn btn-sm btn-primary">Editar
                           </a>
                          </td>
                        </tr>
                        {% endfor %}
                      </tbody>
                      </table>
              </div>
              {% endif %}                     

</div>




{% endblock %}
