{% extends '::frontenda.html.twig' %}
{% block title %}Portada{% endblock %}



{% block article %} 


<div class="container sha well ">


        <a href="{{ path('todoslosserviciosvuelo') }}" class="col-sm-2 btn btn-sm btn-success">
            Lista de Vuelos
        </a>

        <a href="{{ path('vuelo_edit', { 'id': entity.idservicioVuelo }) }}" class="col-sm-2 btn btn-sm btn-primary">
            Editar
        </a>

        <form action="{{ path('vuelo_delete', { 'id': entity.idservicioVuelo }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button class="col-sm-2 btn btn-sm btn-danger" type="submit" onclick="return confirm('¿Esta seguro que desea borrar este servicio?');">Borrar</button>
        </form>
 

<h1 class="col-sm-12 bg-primary">Vuelo</h1>

<table class="table table-striped">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.idservicioVuelo }}</td>
        </tr>
        <tr>
            <th>Compañia </th>
            <td>{{ entity.compania }}</td>
        </tr>
        <tr>
            <th>Foto</th>
            <td>{{ entity.foto }}</td>
        </tr>
        <tr>
            <th>Fecha </th>
            <td>{{ entity.fecha|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Origen</th>
            <td>{{ entity.origen }}</td>
        </tr>
        <tr>
            <th>Hora Salida</th>
            <td>{{ entity.horaSalida|date('H:i:s') }}</td>
        </tr>
        <tr>
            <th>Destino</th>
            <td>{{ entity.destino }}</td>
        </tr>
        <tr>
            <th>Categoria </th>
            <td>{{ entity.categoria }}</td>
        </tr>
        <tr>
            <th>Precio</th>
            <td>{{ entity.precio }}</td>
        </tr>
        <tr>
            <th>Detalles</th>
            <td>{{ entity.detalles }}</td>
        </tr>
        <tr>
            <th>Fechacreacion</th>
            <td>{{ entity.fechaCreacion|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Estado</th>
            <td>{{ entity.estado }}</td>
        </tr>
        <tr>
            <th>Producto</th>
            <td>{{ entity.productoproducto }}</td>
        </tr>
    </tbody>
</table>


        <a href="{{ path('todoslosserviciosvuelo') }}" class="col-sm-2 btn btn-sm btn-success">
            Lista de Vuelos
        </a>
   
        <a href="{{ path('vuelo_edit', { 'id': entity.idservicioVuelo }) }}" class="col-sm-2 btn btn-sm btn-primary">
            Editar
        </a>
   
        <form action="{{ path('vuelo_delete', { 'id': entity.idservicioVuelo }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button class="col-sm-2 btn btn-sm btn-danger" type="submit" onclick="return confirm('¿Esta seguro que desea borrar este servicio?');">Borrar</button>
        </form>
 
</div>
{% endblock %}